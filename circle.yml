dependencies:
  pre:
    - pip install -Ur requirements.txt
    - pip install boto3
test:
  override:
    - python test.py
deployment:
  master:
    branch: master
    commands:
      - make download-base-zip upload
